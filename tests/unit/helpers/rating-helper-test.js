import { ratingHelper } from 'room-review/helpers/rating-helper';
import { module, test } from 'qunit';

module('Unit | Helper | rating helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ratingHelper([42]);
  assert.ok(result);
});
