import { roomPopularity } from 'room-review/helpers/room-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | room popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = roomPopularity([42]);
  assert.ok(result);
});
